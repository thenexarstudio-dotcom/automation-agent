# Security headers live in public/_headers (Netlify reads that file format
# natively from the publish directory) — not duplicated here.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Pin Node to match local dev so the Astro 5 build is reproducible on CI.
  NODE_VERSION = "22"
